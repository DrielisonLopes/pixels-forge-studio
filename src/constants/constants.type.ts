export interface AboutGame {
  title: string;
  description: string;
}

export interface Game {
  name: string;
  externalLink: string;
  imageUrl: string;
  images: string[];
  description: string;
  category: string;
  slug: string;
}
