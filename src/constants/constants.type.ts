export interface AboutGame {
  title: string;
  description: string;
}

export interface Game {
  name: string;
  externalLink: string;
  thumbnail: string;
  banner: string;
  images: string[];
  description: string;
  category: string;
  slug: string;
}
